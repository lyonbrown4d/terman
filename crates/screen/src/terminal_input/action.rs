#[derive(Clone, Debug, PartialEq, Eq)]
pub(crate) enum ScreenInputAction {
    Bytes(Vec<u8>),
    Clear,
    Detach,
    DetachAll,
    DumpTermcap,
    Displays,
    Fit,
    Help,
    Hardcopy,
    Info,
    Kill,
    License,
    LastMessage,
    LastWindow,
    LogToggle,
    NewWindow,
    Number,
    Paste,
    PreviousWindow,
    Quit,
    ReadBuffer,
    RemoveBuffer,
    Resize,
    Redisplay,
    Reset,
    SelectWindow(usize),
    Time,
    Title,
    Version,
    WidthToggle,
    Windows,
    WriteBuffer,
    NextWindow,
}